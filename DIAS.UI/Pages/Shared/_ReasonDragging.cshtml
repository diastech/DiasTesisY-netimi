@model string

@(Html.DevExtreme().List()
    .ID(@Model)
    .Height(380)
    .DataSource(ds => ds.RemoteController()
    .OnBeforeSend("beforeSendReasonList")
    .Key("Id")
    .LoadUrl(Url.Page("RoleDefinition", @Model))
    //.InsertUrl(Url.Page("Insert{0}", @Model))
    //.UpdateUrl(Url.Page("Update{0}", @Model))
    //.DeleteUrl(Url.Page("Delete{0}", @Model))
    )
    .SelectionMode(ListSelectionMode.All)
    .ShowSelectionControls(true)
    .DataSourceOptions(dso => dso.Paginate(false))
    .DisplayExpr("CategoryName")
    .MenuMode(ListMenuMode.Slide)
    .OnSelectionChanged("onSelectionChangedReasonList")
)
